import React from 'react'
import { CalendarApp } from '../../components'

export default function index() {
  return (
    <>
    {/* <CalendarApp date={date} setDate={setDate} title={title} setTitle={setTitle} description={description} setDescription={setDescription} message={message} setMessage={setMessage} /> */}
    <CalendarApp />
    </>
  )
}
